extends layout

block content 
  h1 Become an Admin, #{username} !

  form(method="POST" action='')
    .form-group
      label(for='passcode') Passcode: 
      input#passcode.form-control(type='password' name='passcode' required='true')
    button.btn.btn-primary(type='submit') Ascend
  
  if error 
    p=error