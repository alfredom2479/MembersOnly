extends layout 

block content 
  h1=title 

  form(method='POST' action='')
    .form-group 
      label(for='username') Username: 
      input#username.form-control(type='text' placeholder='Username' name='username' required='true' value=(credentials===undefined ? '': credentials.username))
      label(for='password') Password: 
      input#password.form-control(type='password' name='password' required='true')
    button.btn.btn-primary(type='submit') Log in

  if errors 
    ul 
      for error in errors
        li!=error.msg